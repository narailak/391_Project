PC ros2

ros2 run micro_ros_agent micro_ros_agent serial --dev /dev/ttyUSB0 -b 115200 


// new tab 
export ROS_DOMAIN_ID=96

tao@tao-X556UQK:~$ ros2 topic echo / #เลือก topic 

ros2 topic echo /white_slot/debug/counter 


ros2 launch drive_controller joy_drive.launch.py 

colcon build

sudo usermod -a -G dialout <your_username>

nano ~/.bashrc

 . install/setup.bash


git pull origin main

rm -rf build install log

ip route get 8.8.8.8

/etc/netplan

sudo netplan apply

ping -c 40 8.8.8.8

v4l2-ctl --list-devices

ros2 launch robot_bringup bringup_all.launch.py enable_cams:=true

sudo nmcli general status
sudo nmcli dev status

ros2 launch robot_bringup bringup_all.launch.py enable_cams:=true


ros2 launch dual_cam_mjpeg_server dual_cam_mjpeg.launch.py 
ros2 launch robot_bringup bringup_all.launch.py

ros2 run micro_ros_agent micro_ros_agent serial --dev /dev/esp32_1
ros2 run micro_ros_agent micro_ros_agent serial --dev /dev/esp32_2


sudo usermod -aG dialout $USER
sudo chmod 666 /dev/ttyUSB0




